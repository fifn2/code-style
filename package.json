{
  "name": "root",
  "private": true,
  "devDependencies": {
    "commitizen": "^4.0.3",
    "cz-conventional-changelog": "^3.0.2",
    "lerna": "^3.16.4",
    "semantic-release": "^15.13.24"
  },
  "version": "0.0.0-development",
  "scripts": {
    "build": "echo todo",
    "semantic-release": "lerna exec --concurrency 1 -- npx --no-install semantic-release -e semantic-release-monorepo || true",
    "test": "echo todo"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fifn2/code-style.git"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
